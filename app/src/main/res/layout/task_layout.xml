<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- Task CheckBox -->
        <CheckBox
            android:id="@+id/tdo_checkBox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:buttonTint="@android:color/holo_blue_dark"
            android:paddingStart="8dp"
            android:text="This is a Sample Task inside the application." />

        <!-- Priority TextView -->
        <TextView
            android:id="@+id/tdo_priority"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tdo_checkBox"
            android:layout_marginTop="8dp"
            android:text="Priority: "
            android:textColor="@android:color/black"
            android:textSize="16sp" />

        <!-- Deadline TextView -->
        <TextView
            android:id="@+id/tdo_deadline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tdo_priority"
            android:layout_marginTop="4dp"
            android:text="Deadline: "
            android:textColor="@android:color/black"
            android:textSize="14sp" />

    </RelativeLayout>
</androidx.cardview.widget.CardView>